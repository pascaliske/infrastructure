apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: 'links.{{ domain }}'
  namespace: '{{ role_name }}'
spec:
  secretName: 'links.{{ domain }}'
  dnsNames:
    - 'links.{{ domain }}'
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-lets-encrypt
