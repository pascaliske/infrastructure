apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: 'auth.{{ domain }}'
  namespace: '{{ role_name }}'
spec:
  secretName: 'auth.{{ domain }}'
  dnsNames:
    - 'auth.{{ domain }}'
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-lets-encrypt
