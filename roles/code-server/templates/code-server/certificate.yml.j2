apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: 'code.{{ domain }}'
  namespace: '{{ role_name }}'
spec:
  secretName: 'code.{{ domain }}'
  dnsNames:
    - 'code.{{ domain }}'
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-lets-encrypt
