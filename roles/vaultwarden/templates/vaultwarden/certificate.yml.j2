apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: 'vault.{{ domain }}'
  namespace: '{{ role_name }}'
spec:
  secretName: 'vault.{{ domain }}'
  dnsNames:
    - 'vault.{{ domain }}'
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-lets-encrypt
