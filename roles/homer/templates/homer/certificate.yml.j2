apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: '{{ domain }}'
  namespace: '{{ role_name }}'
spec:
  secretName: '{{ domain }}'
  dnsNames:
    - '{{ domain }}'
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-lets-encrypt
