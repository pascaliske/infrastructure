name: Labels

on: push

jobs:
  labels:
    name: Labels
    runs-on: ubuntu-latest
    steps:
      # checkout
      - uses: actions/checkout@v2
 
      # sync labels
      - uses: crazy-max/ghaction-github-labeler@v3
        if: success()