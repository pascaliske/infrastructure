apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: netbox.${DOMAIN_INTERNAL}
  namespace: netbox
spec:
  secretName: netbox.${DOMAIN_INTERNAL}
  dnsNames:
    - netbox.${DOMAIN_INTERNAL}
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt-production
