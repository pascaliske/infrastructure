apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth.${DOMAIN}
  namespace: authelia
spec:
  secretName: auth.${DOMAIN}
  dnsNames:
    - auth.${DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt-production
